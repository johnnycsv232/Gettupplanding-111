# GETTUPP ZENITH PROTOCOLS

<CRITICAL_INSTRUCTION>
STOP.
READ `skills/using-superpowers/SKILL.md`.
INVOKE `Skill` tool.
DO NOT PROCEED WITHOUT IT.
</CRITICAL_INSTRUCTION>

## ?? The Zenith Mandate (Invariants)
1. **Security**: No secrets in code. No raw queries. 
2. **Quality**: No `any`. No `console.log`. Zod for EVERYTHING.
3. **Workflow**: `npm run verify` is the ONLY Definition of Done.

## ?? Expert Skill Routing
| Task Type | REQUIRED Skill |
|-----------|----------------|
| **Planning** | `@plan-writing` + `@senior-architect` |
| **Testing** | `@tdd-workflow` + `@playwright-skill` |
| **Debugging** | `@systematic-debugging` |
| **UI Design** | `@ui-ux-pro-max` + `@3d-web-experience` |
| **Security** | `@top-web-vulnerabilities` |
